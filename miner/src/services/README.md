# Services folder

# Purpose

The services folder holds **stateless** functions that repersent use-cases in our system (such as starting miner, editing a resume, etc). These functions will be called by the `interfaces` and the functions will react with `core` to achieve their function.
